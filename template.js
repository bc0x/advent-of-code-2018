const parse = require("../dataLoader").parse;

const dayXpart1 = (data) => {
}

const dayXpart2 = (data) => {
  
}

const data = parse(`${__dirname}/data.txt`);
console.log(`Day X Part 1 -- ${dayXpart1(data)}`);
console.log(`Day X Part 2 -- ${dayXpart2(data)}`);
